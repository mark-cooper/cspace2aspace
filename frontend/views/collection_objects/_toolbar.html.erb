<% if user_can?('update_collection_object_record') %>
  <div class="record-toolbar">
    <% if not @collection_object.suppressed %>
      <% if !['edit', 'update'].include?(controller.action_name) %>
        <div class="btn-group pull-left">
          <%= link_to I18n.t("actions.edit"), {:controller => :collection_objects, :action => :edit, :id => @collection_object.id}, :class => "btn btn-sm btn-primary" %>
        </div>
      <% end %>
    <% end %>

    <div class="btn-toolbar pull-right">
      <div class="btn-group">
        <div class="btn btn-inline-form">
          <%= button_delete_action url_for(:controller => :collection_objects, :action => :delete, :id => @collection_object.id), { :"data-title" => I18n.t("actions.delete_confirm_title", :title => title) } %>
        </div>
      </div>
    </div>
    <div class="clearfix"></div>
  </div>
<% end %>
